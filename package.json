{
  "name": "wuxianqin",
  "private": true,
  "version": "0.21.0",
  "description": "無絃琴 · 凌空古琴(Capacitor 壳)",
  "scripts": {
    "build:www": "bash scripts/build-www.sh",
    "ios": "npm run build:www && npx cap sync ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "cordova-plugin-purchase": "^13.12.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
